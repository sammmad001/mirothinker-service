# MiroThinker 质量提升方案 — 全维度优化指南

> 版本: v1.0 | 2026-05-11

---

## 目录

1. [固定信息渠道策略](#1-固定信息渠道策略)
2. [信源可信度分级体系](#2-信源可信度分级体系)
3. [交叉验证机制增强](#3-交叉验证机制增强)
4. [Prompt 工程优化](#4-prompt-工程优化)
5. [后处理质量检查](#5-后处理质量检查)
6. [领域特定优化](#6-领域特定优化)
7. [用户反馈闭环](#7-用户反馈闭环)
8. [知识图谱增强](#8-知识图谱增强)
9. [质量指标体系](#9-质量指标体系)
10. [实施路线图](#10-实施路线图)

---

## 1. 固定信息渠道策略

### 1.1 为什么需要固定渠道？

**问题**: 通用搜索返回的结果质量参差不齐：
- SEO 垃圾内容混入
- 过时信息未被过滤
- 缺乏权威性的博客/自媒体内容
- 同一信源的重复引用

**解决**: 固定高质量信源 + 通用搜索补充

### 1.2 分层信息渠道设计

```
┌────────────────────────────────────────────────────┐
│              信息渠道分层架构                        │
├────────────────────────────────────────────────────┤
│                                                    │
│  L1: 核心信源 (必查, 100% 覆盖)                      │
│  ├── 学术: Google Scholar, arXiv, PubMed           │
│  ├── 技术: GitHub, Stack Overflow, 官方文档         │
│  ├── 新闻: Reuters, AP News, BBC                    │
│  ├── 数据: World Bank, IMF, 国家统计局              │
│  └── 百科: Wikipedia (作为入口，不作为唯一来源)       │
│                                                    │
│  L2: 扩展信源 (按需, 补充深度)                       │
│  ├── 行业报告: Gartner, McKinsey, BCG               │
│  ├── 分析平台: Statista, Our World in Data          │
│  ├── 专业媒体: Nature, Science, MIT Technology Review │
│  └── 论坛: Reddit (特定领域), Hacker News            │
│                                                    │
│  L3: 通用搜索 (兜底, 发现新信源)                     │
│  └── Brave Search / Google Search                   │
│                                                    │
└────────────────────────────────────────────────────┘
```

### 1.3 实现方案

```python
# 固定信源配置
CORE_SOURCES = {
    "academic": {
        "name": "学术信源",
        "urls": [
            "https://scholar.google.com",
            "https://arxiv.org",
            "https://pubmed.ncbi.nlm.nih.gov",
        ],
        "search_template": "site:scholar.google.com OR site:arxiv.org {query}",
        "priority": "highest",
    },
    "tech": {
        "name": "技术信源",
        "urls": [
            "https://github.com",
            "https://stackoverflow.com",
            "https://docs.python.org",
        ],
        "search_template": "site:github.com OR site:stackoverflow.com {query}",
        "priority": "high",
    },
    "news": {
        "name": "新闻信源",
        "urls": [
            "https://www.reuters.com",
            "https://apnews.com",
            "https://www.bbc.com/news",
        ],
        "search_template": "site:reuters.com OR site:apnews.com {query}",
        "priority": "high",
    },
    "data": {
        "name": "数据信源",
        "urls": [
            "https://data.worldbank.org",
            "https://www.imf.org/en/Data",
            "https://data.stats.gov.cn",
        ],
        "search_template": "site:data.worldbank.org OR site:imf.org {query}",
        "priority": "medium",
    },
}

class FixedChannelSearch:
    """固定渠道搜索引擎"""

    async def search(self, query: str, domain: str = None) -> list[dict]:
        """
        按优先级搜索固定渠道
        1. 先搜索核心信源 (L1)
        2. 结果不足时搜索扩展信源 (L2)
        3. 最后用通用搜索兜底 (L3)
        """
        results = []

        # Phase 1: 核心信源
        for source in CORE_SOURCES.values():
            source_results = await self._search_source(
                source["search_template"].format(query=query)
            )
            results.extend([
                {**r, "source_tier": "L1", "source_name": source["name"]}
                for r in source_results
            ])

        # 如果核心信源结果不足
        if len(results) < 10:
            # Phase 2: 通用搜索补充
            general_results = await self.general_search(query)
            results.extend([
                {**r, "source_tier": "L3", "source_name": "通用搜索"}
                for r in general_results
            ])

        return results

    async def _search_source(self, query: str) -> list[dict]:
        """执行具体信源搜索"""
        # 使用 Brave Search 的 site: 操作符
        return await brave_search(query)
```

### 1.4 固定渠道的收益

| 维度 | 通用搜索 | 固定渠道 + 通用搜索 | 提升 |
|------|----------|-------------------|------|
| 信息权威性 | 中 | 高 | **+30%** |
| 结果一致性 | 低 | 高 | **+40%** |
| SEO 垃圾过滤 | 无 | 自动过滤 | **减少 50% 噪音** |
| 引用可追溯性 | 中 | 高 | **+25%** |

---

## 2. 信源可信度分级体系

### 2.1 信源评分模型

```python
class SourceCredibilityScorer:
    """信源可信度评分器"""

    def __init__(self):
        # 预定义信源权重
        self.source_weights = {
            "nature.com": 0.95,
            "science.org": 0.95,
            "arxiv.org": 0.85,
            "reuters.com": 0.90,
            "apnews.com": 0.90,
            "bbc.com": 0.85,
            "github.com": 0.80,
            "wikipedia.org": 0.70,  # 作为入口而非最终来源
            "stackoverflow.com": 0.75,
            "medium.com": 0.50,
            "zhihu.com": 0.45,
        }

    def score(self, url: str, content: str, metadata: dict = None) -> dict:
        """综合评分信源可信度"""
        domain = self._extract_domain(url)

        # 1. 基础权重
        base_weight = self.source_weights.get(domain, 0.50)

        # 2. 内容质量评分
        content_score = self._evaluate_content(content)

        # 3. 时效性评分
        recency_score = self._evaluate_recency(metadata)

        # 4. 引用完整性
        citation_score = self._evaluate_citations(content)

        # 综合评分
        final_score = (
            base_weight * 0.4 +
            content_score * 0.3 +
            recency_score * 0.15 +
            citation_score * 0.15
        )

        return {
            "score": round(final_score, 3),
            "level": self._score_to_level(final_score),
            "breakdown": {
                "base_weight": round(base_weight, 3),
                "content_score": round(content_score, 3),
                "recency_score": round(recency_score, 3),
                "citation_score": round(citation_score, 3),
            }
        }

    def _evaluate_content(self, content: str) -> float:
        """评估内容质量"""
        score = 0.5

        # 有数据支撑
        has_data = bool(re.search(r'\d+[\.,]\d+|%|\$', content))
        if has_data:
            score += 0.15

        # 有引用
        has_citations = bool(re.search(r'\[\d+\]|\(.*?et al\.|according to', content, re.I))
        if has_citations:
            score += 0.15

        # 长度适中 (太短可能不完整，太长可能灌水)
        word_count = len(content.split())
        if 200 < word_count < 3000:
            score += 0.1

        # 有明确作者/机构
        has_author = bool(re.search(r'by \w+|authored by|published by', content, re.I))
        if has_author:
            score += 0.1

        return min(score, 1.0)

    def _evaluate_recency(self, metadata: dict) -> float:
        """评估时效性"""
        if not metadata or "date" not in metadata:
            return 0.5

        try:
            from datetime import datetime
            date = datetime.fromisoformat(metadata["date"])
            days_old = (datetime.now() - date).days

            if days_old < 30:
                return 1.0
            elif days_old < 365:
                return 0.8
            elif days_old < 3 * 365:
                return 0.6
            else:
                return 0.3
        except:
            return 0.5

    def _score_to_level(self, score: float) -> str:
        """评分转等级"""
        if score >= 0.85:
            return "A (权威)"
        elif score >= 0.70:
            return "B (可靠)"
        elif score >= 0.50:
            return "C (一般)"
        else:
            return "D (谨慎)"
```

### 2.2 可信度分级应用

```python
def filter_results_by_credibility(results: list[dict], min_score=0.60) -> list[dict]:
    """按可信度过滤结果"""
    filtered = []
    for r in results:
        if r.get("credibility_score", 0) >= min_score:
            filtered.append(r)
    return filtered

def weight_by_credibility(results: list[dict]) -> list[dict]:
    """按可信度加权排序"""
    return sorted(
        results,
        key=lambda x: x.get("credibility_score", 0),
        reverse=True
    )
```

**效果**:
- 自动过滤低质量信源 (评分 < 0.50)
- 高可信度信源在结果中优先展示
- 最终报告中低可信度内容明确标注

---

## 3. 交叉验证机制增强

### 3.1 多维度交叉验证

```
┌──────────────────────────────────────────────────┐
│              交叉验证框架                          │
├──────────────────────────────────────────────────┤
│                                                  │
│  主张: "AI 将在 2030 年替代 30% 的工作岗位"         │
│    │                                             │
│    ├── 验证 1: 来源独立性                          │
│    │   └── 是否来自同一机构的不同报告？              │
│    │       → 是: 权重降低  → 否: 权重提高           │
│    │                                             │
│    ├── 验证 2: 数据一致性                          │
│    │   └── 各来源的数据是否一致？                   │
│    │       → 一致: 可信度提高  → 矛盾: 需深入分析   │
│    │                                             │
│    ├── 验证 3: 时间合理性                          │
│    │   └── 信息是否为最新？                         │
│    │       → 最新: 权重高  → 过时: 权重低           │
│    │                                             │
│    ├── 验证 4: 利益冲突                            │
│    │   └── 来源是否有利益相关？                      │
│    │       → 无: 权重高  → 有: 标注并降低权重       │
│    │                                             │
│    └── 验证 5: 方法论合理性                        │
│        └── 研究方法是否科学？                       │
│            → 科学: 权重高  → 存疑: 标注不确定性     │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 3.2 矛盾检测与处理

```python
class ContradictionDetector:
    """矛盾检测器"""

    def detect(self, claims: list[dict]) -> list[dict]:
        """检测多个主张之间的矛盾"""
        contradictions = []

        # 按主题分组
        topics = self._group_by_topic(claims)

        for topic, topic_claims in topics.items():
            if len(topic_claims) < 2:
                continue

            # 检测数值矛盾
            numeric_conflicts = self._detect_numeric_conflicts(topic_claims)
            if numeric_conflicts:
                contradictions.append({
                    "topic": topic,
                    "type": "numeric_conflict",
                    "details": numeric_conflicts,
                    "resolution": "需人工判断或寻找第三方来源验证"
                })

            # 检测定性矛盾
            qualitative_conflicts = self._detect_qualitative_conflicts(topic_claims)
            if qualitative_conflicts:
                contradictions.append({
                    "topic": topic,
                    "type": "qualitative_conflict",
                    "details": qualitative_conflicts,
                    "resolution": "报告中需明确说明矛盾双方观点"
                })

        return contradictions

    def _detect_numeric_conflicts(self, claims: list[dict]) -> list[dict]:
        """检测数值矛盾"""
        conflicts = []
        numbers = []

        for claim in claims:
            nums = re.findall(r'(\d+(?:\.\d+)?)\s*(%|million|billion)?', claim.get("text", ""))
            for num, unit in nums:
                numbers.append({
                    "value": float(num),
                    "unit": unit,
                    "source": claim.get("source", ""),
                    "credibility": claim.get("credibility_score", 0)
                })

        # 相同单位但数值差异 > 20%
        for i, n1 in enumerate(numbers):
            for n2 in numbers[i+1:]:
                if n1["unit"] == n2["unit"] and n1["value"] > 0:
                    diff = abs(n1["value"] - n2["value"]) / n1["value"]
                    if diff > 0.20:
                        conflicts.append({
                            "claim1": n1,
                            "claim2": n2,
                            "difference": f"{diff:.0%}"
                        })

        return conflicts
```

### 3.3 矛盾处理策略

| 矛盾类型 | 处理方式 | 输出示例 |
|----------|----------|----------|
| 数值矛盾 (< 30%) | 取加权平均 | "数据在 X-Y 之间浮动" |
| 数值矛盾 (> 30%) | 标注矛盾 | "来源 A 称 X，来源 B 称 Y，差异较大" |
| 定性矛盾 | 双方观点并列 | "部分专家认为...，也有观点指出..." |
| 时效矛盾 | 优先最新 | "最新数据表明 X (2025年)，此前数据为 Y (2023年)" |

---

## 4. Prompt 工程优化

### 4.1 分角色 Prompt 设计

```python
# Phase 1: 研究助手 Prompt (信息收集)
RESEARCHER_PROMPT = """You are a research assistant focused on thorough information gathering.

Rules:
1. Search broadly - aim for 20-25 diverse sources
2. Prioritize primary sources over secondary summaries
3. Note contradictions between sources
4. Extract exact data points and statistics
5. Always record source URLs and publication dates

Output format:
- Source: [title] ([url], [date])
- Key finding: [specific data or claim]
- Credibility: [A/B/C/D]
"""

# Phase 2: 分析专家 Prompt (深度分析)
ANALYST_PROMPT = """You are a senior analyst specializing in [domain].

Your task:
1. Synthesize findings from Phase 1
2. Identify patterns, trends, and anomalies
3. Evaluate competing claims using evidence
4. Highlight areas of consensus vs debate
5. Assess the quality and reliability of each source

Output format:
- Finding: [claim]
- Supporting evidence: [list sources]
- Confidence level: High/Medium/Low
- Caveats: [limitations or uncertainties]
"""

# Phase 3: 审稿人 Prompt (交叉验证)
REVIEWER_PROMPT = """You are a peer reviewer for a research report.

Your task:
1. Fact-check every major claim against original sources
2. Identify any unsupported assertions
3. Check for logical inconsistencies
4. Verify statistics and data points
5. Ensure balanced representation of viewpoints

For each issue found:
- Issue: [description]
- Location: [where in the report]
- Severity: Critical/Major/Minor
- Suggested fix: [recommendation]
"""
```

### 4.2 结构化输出 Prompt

```python
STRUCTURED_OUTPUT_PROMPT = """
Output your findings in this EXACT format:

# [Research Title]

## Executive Summary
[2-3 sentence overview]

## Key Findings

### Finding 1: [Topic]
- Claim: [specific claim with data]
- Evidence:
  - [Source 1]: [quote or data point] (URL, date, credibility: A/B/C/D)
  - [Source 2]: [quote or data point] (URL, date, credibility: A/B/C/D)
- Confidence: High/Medium/Low
- Caveats: [limitations]

### Finding 2: [Topic]
[same format]

## Contradictions & Debates
- [Issue]: [describe conflict between sources]
  - View A: [description] (supported by [sources])
  - View B: [description] (supported by [sources])
- Current consensus: [if any]

## Data Summary
| Metric | Value | Source | Date |
|--------|-------|--------|------|
| [metric] | [value] | [source] | [date] |

## Sources
| # | Title | URL | Date | Credibility |
|---|-------|-----|------|-------------|
| 1 | [title] | [url] | [date] | [A/B/C/D] |
"""
```

---

## 5. 后处理质量检查

### 5.1 质量检查流水线

```python
class QualityCheckPipeline:
    """研究结果质量检查流水线"""

    def __init__(self):
        self.checks = [
            self._check_source_count,
            self._check_citation_completeness,
            self._check_data_support,
            self._check_contradictions,
            self._check_structure,
            self._check_language_quality,
        ]

    def run(self, result: str, metadata: dict) -> dict:
        """运行所有质量检查"""
        issues = []
        scores = {}

        for check in self.checks:
            check_result = check(result, metadata)
            issues.extend(check_result.get("issues", []))
            scores[check.__name__] = check_result.get("score", 0)

        overall_score = sum(scores.values()) / len(scores)

        return {
            "overall_score": round(overall_score, 3),
            "passed": overall_score >= 0.70,
            "scores": scores,
            "issues": issues,
            "recommendation": self._get_recommendation(overall_score),
        }

    def _check_source_count(self, result: str, metadata: dict) -> dict:
        """检查引用来源数量"""
        url_count = result.count("http")
        score = min(url_count / 10, 1.0)  # 目标至少 10 个来源
        issues = []
        if url_count < 5:
            issues.append(f"引用来源过少 ({url_count}个)，建议至少 10 个")
        return {"score": score, "issues": issues}

    def _check_citation_completeness(self, result: str, metadata: dict) -> dict:
        """检查引用完整性"""
        # 有 URL + 有日期 = 完整引用
        complete_citations = len(re.findall(r'http.*?\d{4}', result))
        total_citations = result.count("http")
        completeness = complete_citations / max(total_citations, 1)
        issues = []
        if completeness < 0.5:
            issues.append(f"引用不完整，{int((1-completeness)*100)}% 的引用缺少日期信息")
        return {"score": completeness, "issues": issues}

    def _check_data_support(self, result: str, metadata: dict) -> dict:
        """检查数据支撑"""
        has_data = bool(re.search(r'\d+[\.,]\d+|%|\$', result))
        data_density = len(re.findall(r'\d+', result)) / max(len(result.split()), 1)
        score = min(data_density * 100, 1.0) if has_data else 0.3
        issues = []
        if not has_data:
            issues.append("缺少数据支撑，多为定性描述")
        return {"score": score, "issues": issues}

    def _check_contradictions(self, result: str, metadata: dict) -> dict:
        """检查矛盾处理"""
        has_contradictions_section = "矛盾" in result or "contradiction" in result.lower()
        has_debate_section = "debate" in result.lower() or "分歧" in result
        score = 0.7 if (has_contradictions_section or has_debate_section) else 0.4
        issues = []
        if not has_contradictions_section and not has_debate_section:
            issues.append("未明确标注来源之间的矛盾")
        return {"score": score, "issues": issues}

    def _check_structure(self, result: str, metadata: dict) -> dict:
        """检查结构完整性"""
        required_sections = ["summary", "finding", "source"]
        present = [s for s in required_sections if s in result.lower()]
        score = len(present) / len(required_sections)
        missing = [s for s in required_sections if s not in result.lower()]
        issues = [f"缺少必要章节: {', '.join(missing)}"] if missing else []
        return {"score": score, "issues": issues}

    def _check_language_quality(self, result: str, metadata: dict) -> dict:
        """检查语言质量"""
        # 简单启发式: 句子长度变化、连接词使用
        sentences = re.split(r'[.!?。！？]', result)
        avg_length = sum(len(s.split()) for s in sentences) / max(len(sentences), 1)
        has_transitions = bool(re.search(r'however|therefore|moreover|然而|因此|此外', result, re.I))
        score = 0.6
        if 10 < avg_length < 30:
            score += 0.2
        if has_transitions:
            score += 0.2
        return {"score": min(score, 1.0), "issues": []}

    def _get_recommendation(self, score: float) -> str:
        if score >= 0.85:
            return "质量优秀，可直接交付"
        elif score >= 0.70:
            return "质量良好，建议人工复核"
        elif score >= 0.50:
            return "质量一般，建议重新研究或大幅修改"
        else:
            return "质量较差，建议重新执行研究流程"
```

---

## 6. 领域特定优化

### 6.1 领域识别与路由

```python
DOMAIN_CONFIGS = {
    "tech": {
        "name": "技术/科技",
        "core_sources": ["github.com", "stackoverflow.com", "docs.python.org", "arxiv.org"],
        "search_weight": {"technical": 0.6, "academic": 0.3, "news": 0.1},
        "prompt_variant": "technical",
    },
    "finance": {
        "name": "金融/经济",
        "core_sources": ["reuters.com", "bloomberg.com", "imf.org", "worldbank.org"],
        "search_weight": {"data": 0.4, "news": 0.3, "academic": 0.3},
        "prompt_variant": "data_driven",
    },
    "health": {
        "name": "医疗/健康",
        "core_sources": ["pubmed.ncbi.nlm.nih.gov", "nature.com", "who.int"],
        "search_weight": {"academic": 0.5, "news": 0.2, "data": 0.3},
        "prompt_variant": "evidence_based",
    },
    "general": {
        "name": "综合",
        "core_sources": [],
        "search_weight": {"general": 0.4, "academic": 0.3, "news": 0.3},
        "prompt_variant": "general",
    },
}

def detect_domain(query: str) -> str:
    """自动识别查询领域"""
    tech_keywords = ["AI", "机器学习", "深度学习", "Python", "软件", "算法", "区块链"]
    finance_keywords = ["经济", "金融", "GDP", "通胀", "投资", "股市", "市场"]
    health_keywords = ["医疗", "健康", "疾病", "药物", "治疗", "疫苗", "临床"]

    score = {"tech": 0, "finance": 0, "health": 0}

    for kw in tech_keywords:
        if kw.lower() in query.lower():
            score["tech"] += 1
    for kw in finance_keywords:
        if kw.lower() in query.lower():
            score["finance"] += 1
    for kw in health_keywords:
        if kw.lower() in query.lower():
            score["health"] += 1

    max_domain = max(score, key=score.get)
    return max_domain if score[max_domain] > 0 else "general"
```

### 6.2 领域特定 Prompt

```python
# 技术领域 Prompt 额外指令
TECH_EXTRA = """
Technical research requirements:
- Include code examples or implementation details where relevant
- Reference official documentation and RFCs
- Note version compatibility and deprecation status
- Compare alternatives with trade-offs
"""

# 金融领域 Prompt 额外指令
FINANCE_EXTRA = """
Financial research requirements:
- Always include data with dates and sources
- Distinguish between nominal and real values
- Note inflation adjustments where applicable
- Provide historical context for current figures
- Use official data from government/international organizations
"""

# 医疗领域 Prompt 额外指令
HEALTH_EXTRA = """
Medical research requirements:
- Only cite peer-reviewed studies or official guidelines
- Clearly distinguish between correlation and causation
- Note sample sizes and study limitations
- Indicate level of evidence (RCT, observational, expert opinion)
- Never give medical advice - only report findings
"""
```

---

## 7. 用户反馈闭环

### 7.1 反馈收集

```python
class FeedbackCollector:
    """用户反馈收集器"""

    def collect(self, task_id: str, feedback: dict):
        """收集用户对研究结果的反馈"""
        # feedback = {
        #     "rating": 1-5,
        #     "useful": ["finding1", "finding3"],
        #     "useless": ["finding2"],
        #     "missing": ["what the user expected but didn't get"],
        #     "incorrect": ["factual errors found"],
        #     "comments": "free text"
        # }

        self._store_feedback(task_id, feedback)
        self._update_quality_model(feedback)

    def _update_quality_model(self, feedback: dict):
        """根据反馈优化质量模型"""
        # 如果用户标记某些内容"不准确"
        for incorrect_item in feedback.get("incorrect", []):
            self._downgrade_source_patterns(incorrect_item)

        # 如果用户标记缺少某些内容
        for missing_item in feedback.get("missing", []):
            self._add_requirement_pattern(missing_item)
```

### 7.2 质量迭代

```
用户提交研究 → Agent 研究 → 输出结果 → 用户评分
    ↑                                              │
    │                                              ▼
    │                                    反馈是否负面?
    │                                    ├── 是 → 触发重新研究
    │                                    │        (使用反馈改进)
    │                                    │
    │                                    └── 否 → 标记为高质量样本
    │                                             (加入训练集)
    │
    └── 模型学习 ←── 积累反馈数据
        ├── 优化搜索策略
        ├── 调整信源权重
        └── 改进 Prompt
```

---

## 8. 知识图谱增强

### 8.1 实体识别与关联

```python
class KnowledgeGraphEnhancer:
    """知识图谱增强"""

    def __init__(self):
        self.entities = {}  # 实体 → 属性
        self.relations = []  # (实体1, 关系, 实体2)

    def extract_and_enrich(self, research_result: str) -> dict:
        """从研究结果中提取实体并增强"""
        # 1. 提取关键实体
        entities = self._extract_entities(research_result)

        # 2. 建立关联
        relations = self._build_relations(entities)

        # 3. 发现缺失信息
        gaps = self._find_gaps(entities, relations)

        return {
            "entities": entities,
            "relations": relations,
            "gaps": gaps,
            "enriched_result": self._enrich_with_context(research_result, entities),
        }

    def _find_gaps(self, entities: dict, relations: list) -> list:
        """发现研究中的信息缺口"""
        gaps = []
        for entity, attrs in entities.items():
            if "date" not in attrs:
                gaps.append(f"实体 '{entity}' 缺少时间信息")
            if "source" not in attrs:
                gaps.append(f"实体 '{entity}' 缺少来源信息")
            if "data" not in attrs:
                gaps.append(f"实体 '{entity}' 缺少量化数据")
        return gaps
```

### 8.2 缺口驱动的补充研究

```python
def gap_driven_supplementary_research(initial_result: dict) -> dict:
    """基于信息缺口的补充研究"""
    gaps = initial_result.get("gaps", [])

    if not gaps:
        return initial_result

    # 为每个缺口生成针对性搜索
    supplementary_results = []
    for gap in gaps:
        search_query = self._gap_to_query(gap)
        result = await targeted_search(search_query, max_sources=3)
        supplementary_results.append(result)

    # 合并补充结果
    return self._merge_results(initial_result, supplementary_results)
```

---

## 9. 质量指标体系

### 9.1 质量评分维度

| 维度 | 权重 | 检查项 | 合格线 |
|------|------|--------|--------|
| **信息覆盖** | 20% | 来源数量、领域覆盖、时间跨度 | ≥ 10 来源 |
| **信源可信度** | 25% | A/B 级信源占比、引用完整性 | ≥ 60% A/B |
| **数据支撑** | 15% | 数据密度、数值一致性 | 有具体数据 |
| **交叉验证** | 15% | 矛盾标注、多方观点 | 有标注 |
| **结构完整** | 10% | 必要章节、格式规范 | 全部包含 |
| **语言质量** | 10% | 逻辑连贯、连接词使用 | 有过渡 |
| **可追溯性** | 5% | URL 有效性、日期标注 | ≥ 80% 有效 |

### 9.2 质量等级定义

| 等级 | 分数 | 特征 | 适用场景 |
|------|------|------|----------|
| **S (卓越)** | ≥ 0.90 | 多源验证，数据详实，结构完美 | 直接用于决策 |
| **A (优秀)** | 0.80-0.89 | 充分验证，有数据支撑 | 报告/论文引用 |
| **B (良好)** | 0.70-0.79 | 基本验证，部分数据 | 一般参考 |
| **C (合格)** | 0.60-0.69 | 最少验证，定性为主 | 初步了解 |
| **D (不合格)** | < 0.60 | 验证不足，信息不足 | 需重新研究 |

---

## 10. 实施路线图

### 阶段一: 基础质量保障 (1-2 天)

- [ ] 实现固定渠道搜索 (L1 核心信源)
- [ ] 实现信源可信度评分器
- [ ] 实现结构化输出 Prompt
- [ ] 实现基础质量检查流水线

**预期提升**: 结果质量从 75% → 85%

### 阶段二: 质量增强 (3-5 天)

- [ ] 实现交叉验证框架 (5 维度)
- [ ] 实现矛盾检测与处理
- [ ] 实现领域特定 Prompt
- [ ] 实现用户反馈收集

**预期提升**: 结果质量从 85% → 90%

### 阶段三: 智能优化 (5-10 天)

- [ ] 实现知识图谱增强
- [ ] 实现缺口驱动的补充研究
- [ ] 实现反馈驱动的模型迭代
- [ ] 建立质量指标监控面板

**预期提升**: 结果质量从 90% → 95%

---

*本文档定义了 MiroThinker 质量提升的全维度方案，建议按阶段逐步实施。*
