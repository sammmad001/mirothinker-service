# 搜索和抓取服务自研替代方案

> 月调用量: 10,000+ 次  
> 目标: 降低 80-95% 成本

---

## 一、当前成本分析 (月 10,000 次)

### Serper 搜索成本

| 服务 | 单次调用 | 月调用次数 | 月成本 |
|------|---------|-----------|--------|
| **Serper** | $0.05/次 | 250 次/研究 × 10,000 次 = 2,500,000 次 | **$125,000/月** |

**结论: 完全不可行！**

### Jina 抓取成本  

| 服务 | 单次调用 | 月调用次数 | 月成本 |
|------|---------|-----------|--------|
| **Jina** | $0.01/次 | 150 次/研究 × 10,000 次 = 1,500,000 次 | **$15,000/月** |

**结论: 成本太高！**

---

## 二、替代方案对比

### 方案 A: Brave Search API (搜索替代)

| 项目 | Serper | Brave Search |
|------|--------|--------------|
| 单价 | $0.05/次 | $0.005/次 (Web API) |
| 月 250 万次 | $125,000 | **$12,500** |
| 降低幅度 | - | **90%** |

**优势**:
- 直接替代，代码改动最小
- 搜索结果质量好
- 有免费额度 (2,000 次/月)

**劣势**:
- 仍然有成本 ($12,500/月)
- 依赖第三方服务

**实施难度**: ⭐ (1小时)

---

### 方案 B: 自建 Google Search 爬虫 (搜索替代)

#### 技术架构

```python
# 使用 Google Custom Search API (免费)
# 或 Selenium/Playwright 爬取搜索结果

import httpx
from bs4 import BeautifulSoup

async def google_search_free(query: str, num_results: int = 10) -> list[dict]:
    """使用免费的搜索方案"""
    # 方案 1: Google Custom Search API (免费 100 次/天)
    # 方案 2: DuckDuckGo API (免费，无限制)
    # 方案 3: SearXNG 自建搜索引擎 (开源，免费)
    pass
```

#### 选项 1: DuckDuckGo 免费 API

```python
from duckduckgo_search import DDGS

async def ddg_search(query: str, max_results: int = 10):
    with DDGS() as ddgs:
        results = [r for r in ddgs.text(query, max_results=max_results)]
    return results
```

**成本**: ¥0 (完全免费)  
**限制**: 无官方限制，但高频可能被封 IP  
**质量**: 中等 (不如 Google)

#### 选项 2: SearXNG 自建搜索引擎

```
┌────────────────────────────────────────┐
│         自建 SearXNG 搜索引擎            │
├────────────────────────────────────────┤
│                                        │
│  部署: Docker 一键部署                  │
│  成本: 服务器费用 (~¥50/月)             │
│  搜索源: Google + Bing + DuckDuckGo    │
│  限制: 无 (自己控制)                    │
│  质量: 高 (聚合多源)                    │
│                                        │
│  月成本: ¥50 (服务器)                   │
│  vs Serper: ¥125,000 → ¥50             │
│  节省: 99.96%                          │
│                                        │
└────────────────────────────────────────┘
```

**部署命令**:
```bash
docker run -d -p 8080:8080 searxng/searxng
```

**API 调用**:
```python
async def searxng_search(query: str):
    async with httpx.AsyncClient() as client:
        resp = await client.get(
            "http://localhost:8080/search",
            params={"q": query, "format": "json"}
        )
    return resp.json()["results"]
```

---

### 方案 C: 自建网页抓取 (Jina 替代)

#### 选项 1: Playwright 本地抓取

```python
from playwright.async_api import async_playwright

async def scrape_with_playwright(url: str) -> str:
    """使用 Playwright 抓取网页"""
    async with async_playwright() as p:
        browser = await p.chromium.launch(headless=True)
        page = await browser.new_page()
        await page.goto(url, wait_until="domcontentloaded")
        
        # 提取正文内容
        content = await page.evaluate("""() => {
            // 移除广告、导航等
            document.querySelectorAll('nav, footer, header, script, style')
                .forEach(e => e.remove());
            return document.body.innerText;
        }""")
        
        await browser.close()
        return content[:5000]  # 限制长度
```

**成本**: 
- 服务器: ¥50-100/月 (已有服务器可复用)
- 抓取: ¥0 (免费)

**限制**:
- 并发有限 (取决于服务器配置)
- 部分网站有反爬

#### 选项 2: Trafilatura 轻量级抓取

```python
import trafilatura

def scrape_with_trafilatura(url: str) -> str:
    """轻量级抓取，无需浏览器"""
    downloaded = trafilatura.fetch_url(url)
    if downloaded:
        content = trafilatura.extract(downloaded, include_comments=False)
        return content or ""
    return ""
```

**优势**:
- 超轻量 (无浏览器)
- 速度快 (1-2 秒/页)
- 并发高 (100+ QPS)

**成本**: ¥0

---

## 三、推荐方案: 全自研架构

### 架构设计

```
┌──────────────────────────────────────────────────┐
│           MiroThinker 搜索和抓取架构               │
├──────────────────────────────────────────────────┤
│                                                  │
│  搜索层 (替代 Serper)                             │
│  ├── SearXNG (主) - Docker 部署，聚合多源搜索     │
│  ├── DuckDuckGo (备) - 免费 API，无限制          │
│  └── Brave API (可选) - 高质量付费搜索            │
│                                                  │
│  抓取层 (替代 Jina)                               │
│  ├── Trafilatura (主) - 轻量级，快速             │
│  ├── Playwright (备) - 复杂页面，JS 渲染          │
│  └── Readability (可选) - 正文提取               │
│                                                  │
│  缓存层 (优化)                                    │
│  ├── Redis 缓存搜索结果 (TTL: 24h)               │
│  └── 本地缓存抓取内容 (TTL: 7d)                   │
│                                                  │
└──────────────────────────────────────────────────┘
```

### 成本对比

| 方案 | 月成本 (10,000 次研究) | 对比 Serper+Jina |
|------|----------------------|-----------------|
| **Serper + Jina** | $140,000 (~¥1,000,000) | 基准 |
| **Brave + Playwright** | $12,600 (~¥90,000) | -91% |
| **SearXNG + Trafilatura** | ¥200 (服务器) | **-99.98%** |

---

## 四、按你要求的研究轮数调整

### 新轮数配置

```
Phase 1: 浅层研究    15-25 轮  (原 50 轮)
Phase 2: 聚焦研究    20-30 轮  (原 40 轮)  
Phase 3: 交叉验证   30-50 轮  (原 20 轮)
Phase 4: 报告生成   15-20 轮  (原 10 轮)

总计: 80-125 轮  (原 120 轮)
```

### Token 消耗预估 (新配置)

| 阶段 | 轮数范围 | Tokens/轮 | Tokens 总计 | Credits |
|------|---------|-----------|------------|---------|
| 浅层 | 15-25 | ~1,900 | 28,500-47,500 | 228-380 |
| 聚焦 | 20-30 | ~1,900 | 38,000-57,000 | 304-456 |
| 交叉 | 30-50 | ~1,900 | 57,000-95,000 | 456-760 |
| 报告 | 15-20 | ~1,900 | 28,500-38,000 | 228-304 |
| **总计** | **80-125** | - | **152,000-237,500** | **1,216-1,900** |

### 月 10,000 次成本

| 项目 | 单次 Credits | 月 Credits | 月成本 (元) |
|------|-------------|-----------|------------|
| 百炼 LLM | ~1,500 (平均) | 15,000,000 | ¥150,000 |
| **搜索 (SearXNG)** | ¥0 | ¥0 | **¥0** |
| **抓取 (Trafilatura)** | ¥0 | ¥0 | **¥0** |
| **服务器** | - | - | **¥200** |
| **总计** | - | - | **¥150,200/月** |

**vs 使用 Serper+Jina**:
- Serper: ¥900,000/月
- Jina: ¥100,000/月
- **自研节省: ¥849,800/月 (85% 降低)**

---

## 五、实施计划

### 阶段 1: 立即实施 (1 天)

- [ ] 替换 Serper → DuckDuckGo 免费搜索
- [ ] 替换 Jina → Trafilatura 轻量抓取  
- [ ] 测试搜索和抓取质量

**代码改动**: 仅修改 `ToolClient` 类 (100 行代码)

### 阶段 2: 短期优化 (2-3 天)

- [ ] 部署 SearXNG (Docker)
- [ ] 添加 Redis 缓存层
- [ ] 性能测试和优化

### 阶段 3: 长期稳定 (1 周)

- [ ] 监控和告警
- [ ] 自动降级 (主服务失败时切换备用)
- [ ] 负载均衡 (多实例)

---

## 六、代码实现示例

### 修改 ToolClient 类

```python
class ToolClient:
    """自研搜索和抓取 - 零成本方案"""

    def __init__(self):
        # 不再需要 API keys
        pass

    async def google_search(self, query: str, num_results: int = 10) -> list[dict]:
        """使用 DuckDuckGo 免费搜索"""
        try:
            from duckduckgo_search import DDGS
            with DDGS() as ddgs:
                results = [r for r in ddgs.text(query, max_results=num_results)]
            
            return [{
                "title": r.get("title", ""),
                "link": r.get("href", ""),
                "snippet": r.get("body", "")[:200]
            } for r in results]
        except Exception as e:
            return [{"error": f"Search failed: {str(e)}"}]

    async def scrape_webpage(self, url: str) -> str:
        """使用 Trafilatura 轻量抓取"""
        try:
            import trafilatura
            downloaded = trafilatura.fetch_url(url)
            if downloaded:
                content = trafilatura.extract(downloaded, include_comments=False)
                return (content or "")[:5000]
            return f"Failed to fetch {url}"
        except Exception as e:
            return f"Scrape failed: {str(e)}"
```

### 安装依赖

```bash
pip install duckduckgo-search trafilatura
```

**零 API Key 需求！**

---

## 七、总结

### 推荐方案

| 组件 | 原方案 | 新方案 | 成本降低 |
|------|--------|--------|---------|
| 搜索 | Serper ($0.05/次) | DuckDuckGo (免费) | **100%** |
| 抓取 | Jina ($0.01/次) | Trafilatura (免费) | **100%** |

### 月 10,000 次研究成本

| 项目 | 原方案 | 新方案 |
|------|--------|--------|
| 搜索 + 抓取 | ¥1,000,000 | ¥0 |
| 百炼 LLM | ¥150,000 | ¥150,000 |
| 服务器 | ¥500 | ¥200 |
| **总计** | **¥1,150,500** | **¥150,200** |

**节省: ¥1,000,300/月 (87% 降低)**

---

## 八、立即可执行

```bash
# 1. 安装免费依赖
pip install duckduckgo-search trafilatura

# 2. 修改 ToolClient 类 (替换 Serper 和 Jina)

# 3. 测试
curl -X POST http://127.0.0.1:8000/api/research \
  -d '{"query": "测试搜索", "max_turns": 20}'
```

**无需任何 API Key，完全免费！**
